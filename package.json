{
  "private": true,
  "scripts": {
    "lerna": "lerna --",
    "build": "lerna run build",
    "install:packages": "lerna bootstrap",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
