{
  "private": true,
  "scripts": {
    "lerna": "lerna --",
    "postinstall": "lerna bootstrap",
    "serve:test": "run-p watch:test:unit watch:build:test",
    "serve": "lerna run serve --stream",
    "start": "npm run serve",
    "test": "lerna run test",
    "watch:build:test": "lerna run watch:build:test --silent --stream",
    "watch:test:unit": "ava packages/*/test/out-tsc/test**/*.js -w"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.2"
  }
}
