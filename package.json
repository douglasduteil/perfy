{
  "private": true,
  "scripts": {
    "lerna": "lerna --",
    "postbootstrap": "npm run build",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
